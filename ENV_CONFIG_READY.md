# 📧 إعداد البريد الإلكتروني - mgrdegh90@gmail.com

## ملف .env.local الجاهز

أنشئ ملف `.env.local` في المجلد الرئيسي للمشروع مع المحتوى التالي:

```env
# تفعيل الإرسال الحقيقي (إيقاف وضع المحاكاة)
EMAIL_DEV_MODE="false"

# إعدادات Gmail SMTP
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="mgrdegh90@gmail.com"
SMTP_PASS="YOUR_APP_PASSWORD_HERE"

# عنوان المرسل
EMAIL_FROM="mgrdegh90@gmail.com"
EMAIL_FROM_NAME="RechnungsProfi"

# NextAuth (مطلوب)
NEXTAUTH_SECRET="rechnungsprofi-email-verification-secret-key-2024-very-long-string"
NEXTAUTH_URL="http://localhost:3000"

# اختياري: نسخة احتياطية للإرسال
EMAIL_CC=""
EMAIL_REPLY_TO="mgrdegh90@gmail.com"
```

## 🔑 خطوات إنشاء App Password لـ mgrdegh90@gmail.com

### 1. تسجيل الدخول
- اذهب إلى: https://myaccount.google.com
- سجل دخول بحساب `mgrdegh90@gmail.com`

### 2. تفعيل التحقق بخطوتين
- اضغط على "الأمان" (Security)
- فعّل "التحقق بخطوتين" (2-Step Verification) إذا لم يكن مفعلاً

### 3. إنشاء App Password
- في صفحة الأمان، ابحث عن "App passwords"
- اضغط على "App passwords"
- اختر "Mail" أو "Other" واكتب "RechnungsProfi"
- اضغط "Generate"

### 4. نسخ كلمة المرور
- ستظهر كلمة مرور مكونة من 16 رقم مثل: `abcd efgh ijkl mnop`
- انسخها واستبدل `YOUR_APP_PASSWORD_HERE` بها في الملف أعلاه

## 🚀 تشغيل النظام

### 1. إنشاء الملف
```bash
# في المجلد الرئيسي للمشروع
touch .env.local
# ثم الصق المحتوى أعلاه
```

### 2. إعادة تشغيل الخادم
```bash
# إيقاف الخادم
Ctrl+C

# إعادة التشغيل
npm run dev
```

### 3. التحقق من النجاح
ابحث عن هذه الرسائل في Terminal:
```
🔧 Email service running in PRODUCTION MODE
📧 SMTP User: mgr***@gmail.com
✅ SMTP configuration loaded successfully
✅ Email configuration verified successfully
```

## 🧪 اختبار الإرسال

### اختبار سريع:
1. اذهب إلى: `http://localhost:3000/test-email-verification`
2. أدخل أي بريد إلكتروني للاختبار
3. اضغط "Code senden"
4. يجب أن تصل الرسالة خلال 30 ثانية

### اختبار التسجيل الكامل:
1. اذهب إلى: `http://localhost:3000/auth/register`
2. سجل بحساب جديد
3. ستصل رسالة التحقق من `mgrdegh90@gmail.com`

## 📋 مثال على رسالة التحقق

**المرسل:** RechnungsProfi <mgrdegh90@gmail.com>
**الموضوع:** 🔐 E-Mail-Adresse bestätigen - RechnungsProfi
**المحتوى:** رسالة HTML جميلة مع كود 6 أرقام

## 🔧 استكشاف الأخطاء

### إذا لم تعمل:
1. **تحقق من App Password:** يجب أن يكون 16 رقم بدون مسافات
2. **تحقق من التحقق بخطوتين:** يجب أن يكون مفعلاً
3. **تحقق من الملف:** يجب أن يكون اسمه `.env.local` بالضبط
4. **إعادة التشغيل:** يجب إعادة تشغيل الخادم بعد أي تغيير

### رسائل خطأ شائعة:
- `Invalid login` = App Password خاطئ
- `Authentication failed` = البريد أو كلمة المرور خاطئة
- `SMTP configuration missing` = الملف غير موجود أو فارغ

## ✅ النتيجة المتوقعة

بعد الإعداد الصحيح:
- ✅ رسائل التحقق تُرسل من `mgrdegh90@gmail.com`
- ✅ تصل خلال 30 ثانية
- ✅ تظهر في صندوق الوارد أو Spam
- ✅ كود 6 أرقام واضح ومقروء
- ✅ "Erneut senden" يعمل مع كود جديد
