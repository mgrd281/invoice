# 📧 إعداد البريد الإلكتروني - حل مشكلة عدم وصول رسائل التحقق

## 🚨 المشكلة الحالية
النظام يعمل في وضع التطوير (`EMAIL_DEV_MODE="true"`) مما يعني أنه يحاكي إرسال الرسائل فقط ولا يرسلها فعلياً.

## ✅ الحل السريع - Gmail SMTP

### الخطوة 1: إنشاء ملف الإعدادات
أنشئ ملف `.env.local` في المجلد الرئيسي مع المحتوى التالي:

```env
# تعطيل وضع التطوير لإرسال رسائل حقيقية
EMAIL_DEV_MODE="false"

# إعدادات Gmail SMTP
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"

# عنوان المرسل
EMAIL_FROM="your-email@gmail.com"
EMAIL_FROM_NAME="RechnungsProfi"

# NextAuth (مطلوب)
NEXTAUTH_SECRET="your-very-long-random-secret-key-here-minimum-32-characters"
NEXTAUTH_URL="http://localhost:3000"
```

### الخطوة 2: إعداد Gmail App Password

1. **تسجيل الدخول إلى Gmail**
2. **الذهاب إلى إعدادات الحساب**: [myaccount.google.com](https://myaccount.google.com)
3. **الأمان (Security)** → **التحقق بخطوتين (2-Step Verification)** (يجب تفعيله أولاً)
4. **كلمات مرور التطبيقات (App Passwords)**
5. **إنشاء كلمة مرور جديدة** للتطبيق
6. **نسخ كلمة المرور** ووضعها في `SMTP_PASS`

### الخطوة 3: استبدال بياناتك
```env
SMTP_USER="your-actual-email@gmail.com"
SMTP_PASS="your-16-character-app-password"
EMAIL_FROM="your-actual-email@gmail.com"
```

## 🔄 إعادة تشغيل الخادم
بعد إنشاء الملف:
```bash
# إيقاف الخادم (Ctrl+C)
# ثم إعادة التشغيل
npm run dev
```

## 🧪 اختبار النظام

### 1. فحص الإعدادات
زيارة: `http://localhost:3000/test-email-verification`

### 2. اختبار الإرسال
- أدخل بريدك الإلكتروني
- اضغط "Code senden"
- يجب أن تصل الرسالة خلال 30 ثانية

### 3. اختبار التسجيل الكامل
- اذهب إلى `/auth/register`
- سجل بحساب جديد
- يجب أن تصل رسالة التحقق فوراً

## 🚀 حلول بديلة (للإنتاج)

### Resend (موصى به للإنتاج)
```env
EMAIL_DEV_MODE="false"
RESEND_API_KEY="re_your_api_key_here"
RESEND_FROM_EMAIL="noreply@yourdomain.com"
```

### Outlook/Hotmail SMTP
```env
SMTP_HOST="smtp-mail.outlook.com"
SMTP_PORT="587"
SMTP_USER="your-email@outlook.com"
SMTP_PASS="your-password"
```

## 🔍 استكشاف الأخطاء

### إذا لم تصل الرسائل:
1. **تحقق من Console**: ابحث عن رسائل الخطأ
2. **تحقق من Spam**: قد تكون في البريد المزعج
3. **تحقق من App Password**: تأكد من صحتها
4. **تحقق من 2FA**: يجب تفعيل التحقق بخطوتين

### رسائل الخطأ الشائعة:
- `Invalid login`: كلمة مرور التطبيق خاطئة
- `Less secure app`: استخدم App Password بدلاً من كلمة المرور العادية
- `Authentication failed`: تحقق من البريد وكلمة المرور

## ✅ معايير النجاح
- ✅ تصل الرسالة خلال دقيقتين
- ✅ يعمل "Erneut senden" مع كود جديد
- ✅ لا يمكن الدخول قبل التحقق
- ✅ التحقق ينجح مع الكود الصحيح
