# 🚀 دليل نشر المشروع على Vercel

نعم، يمكنك نشر هذا المشروع بسهولة على **Vercel** لأنه مبني باستخدام **Next.js** (وهو الإطار الذي طورته Vercel).

إليك الخطوات التفصيلية لنشر مشروعك:

## 1. التحضير (قاعدة البيانات) 🗄️
بما أن مشروعك يستخدم **Prisma** مع **PostgreSQL**، ستحتاج إلى قاعدة بيانات سحابية (Cloud Database).
أسهل خيار هو استخدام **Vercel Postgres** أو **Neon** أو **Supabase**.

### الخيار الأسهل: Vercel Postgres
عند إنشاء المشروع على Vercel، يمكنك إضافة قاعدة بيانات "Storage" بضغطة زر.

## 2. ربط GitHub بـ Vercel 🔗
1. اذهب إلى [Vercel Dashboard](https://vercel.com/dashboard).
2. اضغط على **"Add New..."** ثم **"Project"**.
3. اختر **"Import"** بجانب مستودع GitHub الخاص بك (`invoice`).

## 3. إعداد المتغيرات البيئية (Environment Variables) 🔑
في شاشة الإعداد (Configure Project)، ستحتاج إلى إضافة المتغيرات الموجودة في ملف `.env.local`.
اضغط على قسم **"Environment Variables"** وأضف التالي:

| الاسم | القيمة (مثال) | ملاحظات |
|-------|---------------|---------|
| `NEXTAUTH_URL` | `https://your-project.vercel.app` | رابط موقعك بعد النشر |
| `NEXTAUTH_SECRET` | `(سلسلة عشوائية طويلة)` | يمكنك توليدها بأمر `openssl rand -base64 32` |
| `SHOPIFY_ACCESS_TOKEN` | `shpat_...` | مفتاح Shopify الخاص بك |
| `SHOPIFY_SHOP_DOMAIN` | `45dv93-bk.myshopify.com` | رابط متجرك |
| `EMAIL_HOST` | `smtp.office365.com` | إعدادات البريد |
| `EMAIL_USER` | `impressum@karinex.de` | بريد الإرسال |
| `EMAIL_PASS` | `(كلمة المرور)` | كلمة مرور التطبيق |
| `DATABASE_URL` | `postgres://...` | **سيتم تعيينها تلقائياً إذا استخدمت Vercel Postgres** |

## 4. النشر (Deploy) 🚀
1. اضغط على **"Deploy"**.
2. انتظر حتى تنتهي عملية البناء (Build).
3. إذا استخدمت Vercel Postgres، قد تحتاج للذهاب إلى تبويب **"Storage"** وربط قاعدة البيانات بالمشروع.

## 5. تهيئة قاعدة البيانات (بعد النشر) 🛠️
بعد النشر، ستحتاج إلى إنشاء الجداول في قاعدة البيانات السحابية.
يمكنك فعل ذلك من جهازك المحلي عن طريق تغيير `DATABASE_URL` في ملف `.env` مؤقتاً ليشير إلى قاعدة البيانات السحابية، ثم تشغيل:
```bash
npx prisma db push
```
أو يمكنك إضافة هذا الأمر إلى "Build Command" في إعدادات Vercel (خيار متقدم):
`prisma generate && prisma db push && next build`

## ✅ ملخص
1. المشروع جاهز ومتوافق تماماً مع Vercel.
2. الكود موجود الآن على GitHub.
3. فقط اربط المستودع بـ Vercel وأضف المتغيرات البيئية.

حظاً موفقاً في النشر! 🎉
